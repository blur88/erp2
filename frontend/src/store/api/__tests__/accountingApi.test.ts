import { describe, expect, it } from 'vitest'

import { accountingApiSlice } from '@/store/api/accountingApi'

describe('accountingApiSlice', () => {
  it('defines consolidated accounting endpoints', () => {
    expect(accountingApiSlice.endpoints.getChartOfAccounts).toBeDefined()
    expect(accountingApiSlice.endpoints.getChartOfAccountsHierarchy).toBeDefined()
    expect(accountingApiSlice.endpoints.getDeletedChartOfAccounts).toBeDefined()
    expect(accountingApiSlice.endpoints.getChartOfAccount).toBeDefined()
    expect(accountingApiSlice.endpoints.createChartOfAccount).toBeDefined()
    expect(accountingApiSlice.endpoints.updateChartOfAccount).toBeDefined()
    expect(accountingApiSlice.endpoints.deleteChartOfAccount).toBeDefined()
    expect(accountingApiSlice.endpoints.restoreChartOfAccount).toBeDefined()
    expect(accountingApiSlice.endpoints.bulkRestoreChartOfAccounts).toBeDefined()
    expect(accountingApiSlice.endpoints.permanentDeleteChartOfAccount).toBeDefined()
    expect(accountingApiSlice.endpoints.bulkPermanentDeleteChartOfAccounts).toBeDefined()
    expect(accountingApiSlice.endpoints.seedDefaultChartOfAccounts).toBeDefined()
    expect(accountingApiSlice.endpoints.getJournalEntries).toBeDefined()
    expect(accountingApiSlice.endpoints.getJournalEntry).toBeDefined()
    expect(accountingApiSlice.endpoints.createJournalEntry).toBeDefined()
    expect(accountingApiSlice.endpoints.updateJournalEntry).toBeDefined()
    expect(accountingApiSlice.endpoints.deleteJournalEntry).toBeDefined()
    expect(accountingApiSlice.endpoints.postJournalEntry).toBeDefined()
    expect(accountingApiSlice.endpoints.reverseJournalEntry).toBeDefined()
    expect(accountingApiSlice.endpoints.bulkPostJournalEntries).toBeDefined()
    expect(accountingApiSlice.endpoints.bulkDeleteJournalEntries).toBeDefined()
    expect(accountingApiSlice.endpoints.getFiscalPeriods).toBeDefined()
    expect(accountingApiSlice.endpoints.getCurrentFiscalPeriod).toBeDefined()
    expect(accountingApiSlice.endpoints.createFiscalPeriod).toBeDefined()
    expect(accountingApiSlice.endpoints.updateFiscalPeriod).toBeDefined()
    expect(accountingApiSlice.endpoints.deleteFiscalPeriod).toBeDefined()
    expect(accountingApiSlice.endpoints.closeFiscalPeriod).toBeDefined()
    expect(accountingApiSlice.endpoints.reopenFiscalPeriod).toBeDefined()
    expect(accountingApiSlice.endpoints.generateFiscalPeriods).toBeDefined()
    expect(accountingApiSlice.endpoints.validateFiscalPeriodDate).toBeDefined()
    expect(accountingApiSlice.endpoints.getAccountMappings).toBeDefined()
    expect(accountingApiSlice.endpoints.validateAccountMappings).toBeDefined()
    expect(accountingApiSlice.endpoints.deleteAccountMapping).toBeDefined()
    expect(accountingApiSlice.endpoints.getBankReconciliations).toBeDefined()
    expect(accountingApiSlice.endpoints.getBankReconciliation).toBeDefined()
    expect(accountingApiSlice.endpoints.createBankReconciliation).toBeDefined()
    expect(accountingApiSlice.endpoints.updateBankReconciliation).toBeDefined()
    expect(accountingApiSlice.endpoints.deleteBankReconciliation).toBeDefined()
    expect(accountingApiSlice.endpoints.markBankReconciliationCleared).toBeDefined()
    expect(accountingApiSlice.endpoints.unmarkBankReconciliationCleared).toBeDefined()
    expect(accountingApiSlice.endpoints.completeBankReconciliation).toBeDefined()
    expect(accountingApiSlice.endpoints.reopenBankReconciliation).toBeDefined()
    expect(accountingApiSlice.endpoints.getPaymentMethods).toBeDefined()
    expect(accountingApiSlice.endpoints.getDeletedPaymentMethods).toBeDefined()
    expect(accountingApiSlice.endpoints.createPaymentMethod).toBeDefined()
    expect(accountingApiSlice.endpoints.updatePaymentMethod).toBeDefined()
    expect(accountingApiSlice.endpoints.deletePaymentMethod).toBeDefined()
    expect(accountingApiSlice.endpoints.restorePaymentMethod).toBeDefined()
    expect(accountingApiSlice.endpoints.permanentDeletePaymentMethod).toBeDefined()
    expect(accountingApiSlice.endpoints.getSettlements).toBeDefined()
    expect(accountingApiSlice.endpoints.getPendingSettlementSummary).toBeDefined()
    expect(accountingApiSlice.endpoints.getPendingSettlementPayments).toBeDefined()
    expect(accountingApiSlice.endpoints.createSettlement).toBeDefined()
    expect(accountingApiSlice.endpoints.updateSettlement).toBeDefined()
    expect(accountingApiSlice.endpoints.postSettlement).toBeDefined()
    expect(accountingApiSlice.endpoints.reverseSettlement).toBeDefined()
    expect(accountingApiSlice.endpoints.deleteSettlement).toBeDefined()
    expect(accountingApiSlice.endpoints.restoreSettlement).toBeDefined()
    expect(accountingApiSlice.endpoints.permanentDeleteSettlement).toBeDefined()
    expect(accountingApiSlice.endpoints.getDeletedSettlements).toBeDefined()
    expect(accountingApiSlice.endpoints.getOwnerEquityTransactions).toBeDefined()
    expect(accountingApiSlice.endpoints.createOwnerEquityTransaction).toBeDefined()
    expect(accountingApiSlice.endpoints.updateOwnerEquityTransaction).toBeDefined()
    expect(accountingApiSlice.endpoints.deleteOwnerEquityTransaction).toBeDefined()
    expect(accountingApiSlice.endpoints.postOwnerEquityTransaction).toBeDefined()
    expect(accountingApiSlice.endpoints.getExpenses).toBeDefined()
    expect(accountingApiSlice.endpoints.createExpense).toBeDefined()
    expect(accountingApiSlice.endpoints.updateExpense).toBeDefined()
    expect(accountingApiSlice.endpoints.deleteExpense).toBeDefined()
    expect(accountingApiSlice.endpoints.postExpense).toBeDefined()
    expect(accountingApiSlice.endpoints.bulkPostExpenses).toBeDefined()
    expect(accountingApiSlice.endpoints.bulkDeleteExpenses).toBeDefined()
    expect(accountingApiSlice.endpoints.getTrialBalance).toBeDefined()
    expect(accountingApiSlice.endpoints.getBalanceSheet).toBeDefined()
    expect(accountingApiSlice.endpoints.getProfitAndLoss).toBeDefined()
    expect(accountingApiSlice.endpoints.getGeneralLedger).toBeDefined()
    expect(accountingApiSlice.endpoints.getAccountActivity).toBeDefined()
  })
})
