# jsdom 29.1.0 Upgrade Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bump `jsdom` from 29.0.2 to 29.1.0 in the frontend to gain CSS ratio type support and a `getComputedStyle()` correctness fix.

**Architecture:** Single version pin change in `frontend/package.json`, followed by a lockfile refresh. No code changes required — 29.1.0 is fully backward-compatible with 29.0.2.

**Tech Stack:** Node.js ≥20, npm, jsdom, Vitest

---

### Task 1: Bump jsdom version and refresh lockfile

**Files:**
- Modify: `frontend/package.json` (line 63)
- Modify: `frontend/package-lock.json` (auto-generated)

- [ ] **Step 1: Update the version pin**

In `frontend/package.json`, change line 63 from:

```json
"jsdom": "29.0.2",
```

to:

```json
"jsdom": "29.1.0",
```

- [ ] **Step 2: Install and refresh the lockfile**

```bash
cd frontend && npm install
```

Expected: npm resolves jsdom 29.1.0 and updates `package-lock.json`. No peer dependency errors or ERESOLVE warnings.

- [ ] **Step 3: Verify the installed version**

```bash
cd frontend && npm list jsdom
```

Expected output includes:

```
jsdom@29.1.0
```

- [ ] **Step 4: Run the full frontend test suite**

```bash
cd frontend && npm run test
```

Expected: all tests pass (suite takes ~12 minutes — do not assume hung). Zero regressions.

- [ ] **Step 5: Commit**

```bash
git add frontend/package.json frontend/package-lock.json
git commit -m "chore(deps): bump jsdom from 29.0.2 to 29.1.0

Closes #457"
```
