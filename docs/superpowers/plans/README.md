# Plans

`docs/plans/` is for active work only.

## Status Conventions

- `active`: In-progress plan files stay at `docs/plans/`.
- `completed`: Implemented plans are moved to `docs/plans/completed/`.
- `superseded`: Keep the old file in `docs/plans/completed/` and add `-superseded` to the filename, with a pointer to the replacement plan at the top.
